logger:
  mode: syslog
  level: info
  single:
    path: /tmp/ifman.log
    perm: 0644
  rotate:
    dir: /tmp/ifman
    age: 1296000
    cut_period: 86400

interface:

route:

verify:
  check_period: 60
